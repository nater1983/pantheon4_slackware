patch -Np1 -i $CWD/patch/gnome-settings-daemon/0002-sharing-fix-building-without-systemd.patch || { touch ${SLACK_PANTHEON_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/gnome-settings-daemon/keyboard-Use-ibus-sources-from-locale.patch || { touch ${SLACK_PANTHEON_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/gnome-settings-daemon/keyboard-sync-input-sources-to-accountsservice.patch || { touch ${SLACK_PANTHEON_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
