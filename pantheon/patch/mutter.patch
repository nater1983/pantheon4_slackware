patch -Np1 -i $CWD/patch/mutter/0001-window-actor-Special-case-shaped-Java-windows.patch || { touch ${SLACK_PANTHEON_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/mutter/mutter-42.alpha-disable-tegra.patch || { touch ${SLACK_PANTHEON_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/mutter/0001-meson-Update-the-syprof-wrap-to-47.alpha-and-sysprof.patch || { touch ${SLACK_PANTHEON_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/mutter/0001-gschema-Enable-fractional-scaling-experimental-featu.patch || { touch ${SLACK_PANTHEON_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/mutter/0001-Revert-x11-Use-input-region-from-frame-window-for-de.patch || { touch ${SLACK_PANTHEON_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
