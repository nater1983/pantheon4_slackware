mkdir -p build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${PANTHEON_BUILD_ARGS[@]}" \
    $PANTHEON_OPT_ARGS \
    -Dvapi=true \
    -Dlua51=false \
    -Dtests=false \
    -Dgtk_doc=false
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
