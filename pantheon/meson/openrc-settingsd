mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${PANTHEON_BUILD_ARGS[@]}" \
    $PANTHEON_OPT_ARGS \
    -Dhostname-style=slackware \
    -Dlocale-style=slackware \
    -Dtime-style=slackware \
    -Dopenrc=disabled
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
