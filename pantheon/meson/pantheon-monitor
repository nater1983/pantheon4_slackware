mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${PANTHEON_BUILD_ARGS[@]}" \
    $PANTHEON_OPT_ARGS \
    -Dindicator-wingpanel=disabled \
		-Dnvidia=disabled
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
