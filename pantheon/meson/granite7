mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${PANTHEON_BUILD_ARGS[@]}" \
    $PANTHEON_OPT_ARGS \
    -Dintrospection=true \
    -Ddemos=false \
    -Dgtk-stylesheets=true
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
